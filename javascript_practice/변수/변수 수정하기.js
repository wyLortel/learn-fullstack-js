let change = "바꿔 봥";
change = "바꿧다;" // 바꿀때는 let 다시 안써도 됨


change = null;

let num = 3;
num = num + 3;
numt+=4;



//change = null; 이라고 코드에 적혀 있다면, 그건 자바스크립트 
// 엔진이 자동으로 넣어준 게 아니라 개발자가 의도적으로 
// “여기 값은 없다”라고 표시해둔 것
//변수를 사용하는 이유는 반복된곳에 변수가 사용될때 수정이 용이