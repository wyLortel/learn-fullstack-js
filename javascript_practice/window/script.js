let val;

val = window.outerHeight;
val = window.outerWidth;

val = window.innerHeight;
val = window.innerWidth;

window.scrollY;
window.scrollx;

val = window.location

window.history. // 여러가지 잇음 

window.navigater

console.log(val);
